// JavaScript Document

function productDownloadLog(n) {
	$.post("/Home/ProductDownloadLog", {
		ProductID: n
	}, function() {})
}
var str = "",
	addrli = "",
	provinceid = "",
	GetListView = function() {
		str = "";
		$(".plist").each(function() {
			var n = $(this);
			$(this).find("a").each(function() {
				$(this).attr("class").indexOf("on") > 0 && (str += "&" + $(this).attr("data-oper") + "=" + $(this).attr("data-value"), $.trim($(this).attr("data-oper")) == "province" && (str += "&rownum=" + $(this).attr("rownum")));
				$.trim($(this).attr("class")).indexOf("hover") == 0 && (str += "&" + $(this).attr("data-oper") + "=" + $(this).attr("data-value"))
			})
		})
	},
	currentProductId, regMail;
jQuery.extend({
	getParam: function(n) {
		var i = new RegExp("(^|&)" + n + "=([^&]*)(&|$)", "i"),
			t = window.location.search.substr(1).match(i);
		return t != null ? unescape(decodeURI(t[2])) : null
	}
});
$(function() {
	var n = $("#areaoperation"),
		i = $.getParam("rownum"),
		t;
	i > 1 ? (n.attr("data-type", "shou"), n.prev().height(190)) : (n.attr("data-type", "more"),n.prev().height(38));
	$("#areaoperation").click(function() {
		var n = $(this);
		n.attr("data-type") == "more" ? (n.attr("data-type", "shou"), n.prev().height(190)) : n.attr("data-type") == "shou" && (n.attr("data-type", "more"), n.prev().height(38))
	});
regMail = /^(\w-*\.*)+@(\w-?)+(\.\w{2,})+$/;
		   });